using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuState : GameState
{
    public MenuState(GM gameMaster, GameStateMachine stateMachine, string thisStateName) : base(gameMaster, stateMachine, thisStateName)
    {
    }
}
